[project]
name = "cinesearch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "elasticsearch>=8.12.0,<9.0.0",
    "tabulate>=0.10.0",
]
